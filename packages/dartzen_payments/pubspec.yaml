name: dartzen_payments
version: 0.0.1
description: Payment domain and provider implementations for DartZen (Strapi, Adyen).
resolution: workspace
publish_to: none

environment:
  sdk: ">=3.10.4 <4.0.0"

dependencies:
  dartzen_core:
    path: ../dartzen_core
  dartzen_localization:
    path: ../dartzen_localization
  dartzen_telemetry:
    path: ../dartzen_telemetry
  dartzen_transport:
    path: ../dartzen_transport
  meta: ^1.11.0

dev_dependencies:
  lints: ^6.0.0
  mocktail: ^1.0.4
  test: ^1.25.0
