# 0.0.1

- Initial implementation of the DartZen Identity Domain module.
- `IdentityId` value object with validation.
- `IdentityLifecycle` with explicit states: pending, active, revoked, disabled.
- `Identity` domain aggregate.
- `Role` and `Capability` based authority model.
- Semantic domain error helpers.
- Package-scoped localization support.
