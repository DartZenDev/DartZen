name: dartzen_identity_contract
description: The contract layer for the DartZen identity domain. Exposes serializable models and error contracts universal to client and server.
version: 0.0.1
homepage: https://dartzen.dev
repository: https://github.com/DartZenDev/DartZen
issue_tracker: https://github.com/DartZenDev/DartZen/issues

publish_to: none

environment:
  sdk: '>=3.10.3 <4.0.0'

resolution: workspace

topics:
  - identity
  - contract
  - architecture

dependencies:
  dartzen_core: ^0.0.1
  meta: ^1.11.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
