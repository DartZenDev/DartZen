name: CD

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: false # Disabled until deployment is ready

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Build and Push Docker image
        run: |
          echo "Building and pushing Docker image..."
          # docker build -t gcr.io/${{ secrets.GCP_PROJECT_ID }}/dartzen-server .
          # docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/dartzen-server

      - name: Deploy to Cloud Run
        run: |
          echo "Deploying to Cloud Run..."
          # gcloud run deploy dartzen-server \
          #   --image gcr.io/${{ secrets.GCP_PROJECT_ID }}/dartzen-server \
          #   --platform managed
